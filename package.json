{
  "name": "bittle-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Safety-gated control plane for a Petoi Bittle robot",
  "scripts": {
    "deploy": "./deploy.sh",
    "test": ".venv/bin/python -m pytest tests -q",
    "models": "python3 scripts/build_models.py",
    "dev": ".venv/bin/python -m uvicorn app.main:app --reload --port 8008"
  }
}
